#include "lib.h"

void fillMass(int *mass){	//* функція для заповнення динамічного двомірного масиву
	for (int i = 0; i < N; i++){
		for (int j = 0; j < N; j++){
			*(*(mass + i) + j) = 10 + rand() % 99;
		}
	}	
}

void fillMaxMass(int *mass, int *a){	//* функція для знаходження головної діагоналі динамічного двомірного масиву та створення для нього  динамічного одномірного масиву
for (int i = 0; i < N; i++){
	*(a + i) = *(*(mass + i) + i);
}
}
